Photo to GIF (Giphy-ready)

Batch converts images into 2-frame GIFs so they’re accepted by GIPHY. Adds a tiny pixel change to avoid static detection, processes folders, and moves existing GIFs into the output folder.

Usage
pip install pillow
python script.py

Enter your folder path and the script will create a gifs folder with the results.
